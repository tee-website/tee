import { Menu, MenuButton } from "@chakra-ui/react";

function MenuSelectOption({}: {}) {
  return <></>;
}

export default function MenuSelectComponent({ name }: { name: string }) {
  return (
    <Menu>
      <MenuButton />
    </Menu>
  );
}
